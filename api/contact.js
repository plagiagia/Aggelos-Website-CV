import {wrap,method,body,json,db,rate,text,fail} from '../lib/core.mjs';
export default wrap(async(req,res)=>{method(req,['POST']);const b=await body(req);if(b.website)return json(res,200,{ok:true});const name=text(b.name,120),email=text(b.email,254),subject=text(b.subject,150),message=text(b.message,5000);if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)||message.length<10)fail('Enter a valid email and a message of at least 10 characters.');await rate(req,'contact',5);await db('/rest/v1/enquiries',{method:'POST',body:JSON.stringify({name,email,subject,message})});json(res,201,{ok:true});});
